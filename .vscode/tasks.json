{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Linux",
            "type": "shell",
            "command": "docker build --target export --output type=local,dest=./build .",
        }
    ],
}